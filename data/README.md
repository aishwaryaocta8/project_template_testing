# Data Directory

This directory contains project related data files.

## Structure

- `raw/` - Raw, unprocessed data files
- `processed/` - Processed and cleaned data files

## Usage

Place your raw data files in the `raw/` directory. After preprocessing, save processed data to the `processed/` directory.

